package test;

// import java.time.Duration;

// import org.junit.*;
// import frc.libs.motionprofile.*;
// import frc.libs.motionprofile.IMotionProfile.*;

public class LinearMotionProfileTest {
    // @Test
    // public void BasicTest() {
        
    //     final double targetDistance = 100.0;
    //     final double maxVelocity = 10.0;
    //     final double acceleration = 100.0;
    //     IMotionProfile motionProfile = new LinearMotionProfile(targetDistance, maxVelocity, acceleration, true);

    //     System.out.println("DurationMs: " + motionProfile.duration().toMillis());
    //     System.out.println("time (ms), position, velocity, acceleration");

    //     Segment segment0 = motionProfile.getSegment(Duration.ofSeconds(0));
    //     printSegment(segment0);

    //     Segment segment1 = motionProfile.getSegment(Duration.ofSeconds(1));
    //     printSegment(segment1);

    //     Assert.assertTrue("message", true);
    // }

    // private static void printSegment(Segment segment) {
    //     System.out.println(String.format("%d %.2f %.2f %.2f", segment.time.toMillis(), segment.position, segment.velocity, segment.acceleration));
    // }
}